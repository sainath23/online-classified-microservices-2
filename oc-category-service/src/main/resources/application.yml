server:
  port: 8083
spring:
  application:
    name: category-service
  datasource:
    url: jdbc:mysql://localhost:3306/oc_category?useSSL=false
    username: root
    password: rootroot
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      dialect: org.hibernate.dialect.MySQL5Dialect
